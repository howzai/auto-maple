# Next implementation slice

1. Add player-position smoothing and outlier rejection.
2. Select the player candidate nearest the predicted position instead of the first template match.
3. Add stale-frame and capture-thread watchdog checks.
4. Add configurable game window title and capture FPS.
5. Harden GUI rendering when minimap data is unavailable.
6. Add replay-oriented vision tests using saved frames.
